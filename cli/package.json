{
  "name": "@tokenforge/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "tokenforge": "./bin/tokenforge.mjs"
  },
  "scripts": {
    "tokenforge": "node ./bin/tokenforge.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@tokenforge/risk-core": "*",
    "undici": "^8.10.0"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsx": "^4.20.5",
    "vitest": "^3.2.4"
  }
}
