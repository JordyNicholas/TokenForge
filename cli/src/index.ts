import { RISK_CORE_PLACEHOLDER } from "@tokenforge/risk-core";

/** Placeholder until E2 (#13) implements `tokenforge scan | apply | init`. */
export function cliPlaceholder(): boolean {
  return RISK_CORE_PLACEHOLDER;
}
