/**
 * @deprecated Prefer `collectKeptContent` from `@tokenforge/policy-adapters`.
 * Re-export kept so existing CLI imports keep working.
 */
export {
  collectKeptContent,
  type KeptContent,
} from "@tokenforge/policy-adapters";
