import type { ProviderId } from "@tokenforge/risk-core";
import {
  instructionPathForProvider,
  instructionTitleForProvider,
} from "../adapters";

export { instructionPathForProvider, instructionTitleForProvider };
export type { ProviderId };
