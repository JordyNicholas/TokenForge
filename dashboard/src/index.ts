import { RISK_CORE_PLACEHOLDER } from "@tokenforge/risk-core";

/** Placeholder until E3 (#16) scaffolds the Vite React app. */
export function dashboardPlaceholder(): boolean {
  return RISK_CORE_PLACEHOLDER;
}
