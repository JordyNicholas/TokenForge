# Compact rules

Run **Compact rules preview** from Overview or the command palette.

TokenForge synthesizes a lean managed instruction section from your scan — grouped “do not load” paths without token counts in the file. Review before applying through CLI Fix.

Honesty: this is local policy text, not billed usage or pipeline interception.
