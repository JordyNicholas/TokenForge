{
  "name": "checkout-api-fixture",
  "private": true,
  "version": "0.0.0",
  "description": "TokenForge hybrid-eval-app fixture — not a workspace package.",
  "type": "module",
  "scripts": {
    "test": "node --test src/**/*.test.js"
  }
}
