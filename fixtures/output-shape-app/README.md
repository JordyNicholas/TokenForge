# output-shape-app

Fixture for **#171** — RTK-style output-shape file classes (`test_output`, `ci_log`, `build_artifact`).

Contains test/CI output trees, a pipeline log, and a binary artifact path the heuristic layer should flag without hybrid LLM.
